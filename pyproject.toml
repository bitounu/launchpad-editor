[project]
name = "launchpad-editor"
version = "1.0.0"
description = "Pixel art editor and CLI tools for the Novation Launchpad Mini MK3"
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0",
    "mido>=1.3",
    "python-rtmidi>=1.5",
]
